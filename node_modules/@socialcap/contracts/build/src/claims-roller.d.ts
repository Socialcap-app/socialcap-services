import { PrivateKey, PublicKey } from 'o1js';
import { VotingInstance } from "./claims-voting-factory.js";
export { rollupClaims };
declare function rollupClaims(running: VotingInstance[], payerAccount: PublicKey, payerSecret: PrivateKey): Promise<VotingInstance[]>;
