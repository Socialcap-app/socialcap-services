import { PrivateKey, PublicKey, Field, MerkleMap } from 'o1js';
import { ClaimVotingContract, NullifierProxy } from '../ClaimVotingContract.js';
export declare function sendVote(zkClaim: ClaimVotingContract, sender: {
    puk: PublicKey;
    prk: PrivateKey;
}, // sender and voter MUST be the same!
vote: Field, nullifier: NullifierProxy): Promise<void>;
export declare function addElectorsToNullifier(claimUid: Field, electors: PublicKey[]): MerkleMap;
export declare function getNullifierProxy(nullifier: MerkleMap, electorPuk: PublicKey, claimUid: Field): NullifierProxy;
